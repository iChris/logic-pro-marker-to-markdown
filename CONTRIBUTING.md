# Contributing

## Development Process

### Versioning

- **ALWAYS** bump the version number in `package.json` for every pull request or significant change.
- The version number is displayed in the application footer, so it is critical for tracking deployments.
- Follow semantic versioning (major.minor.patch).
